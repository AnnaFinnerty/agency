import React, {Component} from 'react';

import ModalContext from './context';

import '../../App.css';
import { Modal, Header, Button } from 'semantic-ui-react';

function AgencyModal(props){
    console.log('modal props', props);
    return (
        <div>
            agency modale
        </div>
      );
  
}

export default AgencyModal;